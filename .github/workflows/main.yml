# This is a basic workflow to help you get started with Actions

name: Serverless Deployment

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: npm install depedencies
        run: npm install

      - name: Serverless
        uses: serverless/github-action@v1.53.0
        with:
          args: deploy
